# design

UI design skills. Two slash-commands sharing one reference set.

- `/design:create` — design a UI from scratch
- `/design:critique` — review an existing one and return specific, prioritized feedback

References live at the plugin root (`references/`) and are symlinked into each skill folder.
